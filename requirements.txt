# HTTP client for YouTube Data API v3 calls
requests>=2.31.0

# REST API framework
flask>=3.0.0

# Cross-Origin Resource Sharing — required for browser clients
flask-cors>=4.0.0

# .env file support (credentials can also be exported as env vars)
python-dotenv>=1.0.0

# Production WSGI server (required for Render / cloud deploy)
gunicorn>=21.2.0
